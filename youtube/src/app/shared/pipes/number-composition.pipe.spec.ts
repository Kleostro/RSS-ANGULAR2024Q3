import NumberCompositionPipe from './number-composition.pipe';

describe('NumberCompositionPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberCompositionPipe();
    expect(pipe).toBeTruthy();
  });
});
